# 🤖 FinDocBot

**FinDocBot** — это современная RAG-система (Retrieval-Augmented Generation) на базе чистой архитектуры, предназначенная для семантического поиска и ответов на вопросы по финансовым PDF-документам.

Проект использует локальные LLM через **Ollama** и векторное хранилище **PostgreSQL (pgvector)** для обеспечения конфиденциальности данных и высокой производительности.

---

## 🚀 Основные возможности

- 📁 **Загрузка PDF**: автоматический парсинг и индексация финансовых отчетов.
- 🔍 **Семантический поиск**: поиск релевантных фрагментов текста по смыслу, а не только по ключевым словам.
- 💬 **Контекстный чат**: генерация ответов на вопросы с учетом истории диалога и найденных источников.
- 🏗️ **Clean Architecture**: четкое разделение бизнес-логики, домена и инфраструктуры.
- ⚡ **Высокая скорость**: кеширование эмбеддингов и эффективная обработка PDF.

---

## 🛠 Технологический стек

- **Язык**: Python 3.12+
- **API Framework**: FastAPI
- **LLM/Embeddings**: Ollama (модели `qwen2.5:7b` и `nomic-embed-text`)
- **База данных**: PostgreSQL + pgvector
- **Парсинг PDF**: PyPDF
- **Управление зависимостями**: [uv](https://github.com/astral-sh/uv)
- **Контейнеризация**: Docker & Docker Compose

---

## 🏁 Быстрый старт

### Требования
1. Установленный [Docker](https://www.docker.com/) и Docker Compose.
2. Установленный [uv](https://docs.astral.sh/uv/getting-started/installation/).
3. Установленная [Ollama](https://ollama.com/) (если запускаете Ollama локально, а не в Docker).

### Шаги запуска

1. **Клонируйте репозиторий**:
   ```bash
   git clone https://github.com/your-repo/findocbot.git
   cd findocbot
   ```

2. **Настройте окружение**:
   Установите зависимости и подготовьте виртуальное окружение:
   ```bash
   make sync
   ```

3. **Запустите инфраструктуру**:
   Поднимите PostgreSQL и Ollama через Docker Compose:
   ```bash
   make up
   ```

4. **Загрузите модели в Ollama**:
   Если вы используете Ollama в Docker или локально, убедитесь, что модели загружены:
   ```bash
   docker exec -it findocbot-ollama ollama pull qwen2.5:7b
   docker exec -it findocbot-ollama ollama pull nomic-embed-text
   ```

5. **Запустите API**:
   ```bash
   make dev
   ```
   API будет доступно по адресу: `http://localhost:8000`. Документация Swagger: `http://localhost:8000/docs`.

---

## 📖 API Документация

### Загрузка документа
`POST /documents/upload` — загружает PDF-файл для индексации.

```bash
curl -X POST "http://localhost:8000/documents/upload" \
     -H "Content-Type: multipart/form-data" \
     -F "file=@/path/to/report.pdf"
```

### Поиск по документу
`POST /search` — поиск релевантных фрагментов текста.

```bash
curl -X POST "http://localhost:8000/search" \
     -H "Content-Type: application/json" \
     -d '{"query": "Чистая прибыль за 2023 год", "top_k": 3}'
```

### Ответ на вопрос
`POST /ask` — генерация ответа на основе контекста документов.

```bash
curl -X POST "http://localhost:8000/ask" \
     -H "Content-Type: application/json" \
     -d '{
       "question": "Какая выручка была у компании во втором квартале?",
       "session_id": "user-session-123"
     }'
```

---

## 🏗 Архитектура

Проект построен по принципам **Clean Architecture**:

- `src/findocbot/domain`: Доменные модели и сущности.
- `src/findocbot/use_cases`: Бизнес-логика приложения (интерфейсы и реализация сценариев).
- `src/findocbot/infrastructure`: Внешние реализации (БД, Ollama, парсеры).
- `src/findocbot/adapters`: Внешние интерфейсы (REST API).

---

## 🧪 Тестирование и качество кода

- **Запуск тестов**: `make test`
- **Линтинг и форматирование**: `make lint` / `make fmt`

---

## 📄 Лицензия

Проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для подробностей.
